apiVersion: apps/v1
kind: Deployment
metadata:
  name: checkers-game
  labels:
    app: checkers-game
spec:
  replicas: 2
  selector:
    matchLabels:
      app: checkers-game
  template:
    metadata:
      labels:
        app: checkers-game
    spec:
      containers:
      - name: checkers-game
        image: tomwakhungu/checkers-game:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 5000
          name: http
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 5000
          initialDelaySeconds: 10
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 5000
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: checkers-game-service
  labels:
    app: checkers-game
spec:
  type: NodePort
  selector:
    app: checkers-game
  ports:
  - port: 80
    targetPort: 5000
    nodePort: 30080
    protocol: TCP
    name: http
